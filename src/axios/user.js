import request from "./index.js";
import store from "../store/index.js"

async function login({ username, password, rememberMe }) {
    // TODO: 这里发出request请求
    // const { userIdentity, userAvatar, token } = await request();

}

export { login }
